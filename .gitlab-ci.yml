pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html styles.css additions.css app.js hike.yaml mercantour-route.gpx public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
